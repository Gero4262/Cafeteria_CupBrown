<%- include ../layouts/cabecera.ejs %>
    <br><br>

    <a href="/libros/crear" class="btn btn-primary" type="button">Agregar Producto</a>
    <br><br>
    <table class="table table-ligth">
        <thead class="thead-ligth">
            <tr>
                <th>#</th>
                <th>ID</th> 
                <td>Nombre Producto</td>
                <td>Descripcion</td>
                <td>Imagen</td>
                <td>Precio</td>
                <td>Acciones</td>
            </tr>
        </thead>
            <tbody>
                <% for( let libro = 0; libro < libros.length; libro++ ) { %>
                    <tr>
                        <td>  </td>
                        <td> <%= libros[libro].id %>  </td> 
                        <td> <%= libros[libro].nombre %>  </td>
                        <td>   </td>
                        <td>
                            <img class="img-thumbnail" src="images/<%= libros[libro].imagen %> " width="100" alt="">
                             
                         </td>
                         <td> </td>
                        <td> 
                            

                            <div class="btn-group-vertical" role="group" aria-label="Vertical button group">
                                <form class="form-inline" method="get" action="/libros/editar/<%=libros[libro].id  %> ">
                                   <button class="btn btn-warning " type="submit">Editar </button>
                               </form>

                           </div>
                           |
                            <div class="btn-group-vertical" role="group" aria-label="Vertical button group">
                                 <form class="form-inline" method="post" action="/libros/eliminar/<%=libros[libro].id  %> ">
                                    <button class="btn btn-danger" type="submit">Borrar </button>
                                </form>

                            </div>
                         </td>
                    </tr>

                <% } %>

            </tbody>
    </table>

    <%- include ../layouts/pie.ejs %>